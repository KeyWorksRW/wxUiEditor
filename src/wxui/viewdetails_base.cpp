///////////////////////////////////////////////////////////////////////////////
// <auto-generated>
// Code generated by wxUiEditor - see https://github.com/KeyWorksRW/wxUiEditor/
//
// DO NOT EDIT ANYTHING IN THIS FILE UNTIL AFTER THE </auto-generated> MARKER
// All changes before that marker will be lost if the code is re-generated.
///////////////////////////////////////////////////////////////////////////////

// clang-format off
// NOLINTBEGIN
// cppcheck-suppress-begin *

#include <wx/button.h>
#include <wx/persist.h>
#include <wx/persist/splitter.h>
#include <wx/persist/toplevel.h>
#include <wx/sizer.h>

#include "viewdetails_base.h"

bool ViewDetails_base::Create(wxWindow* parent, wxWindowID id, const wxString& title,
    const wxPoint& pos, const wxSize& size, long style, const wxString &name)
{
    // Scaling of pos and size are handled after the dialog
    // has been created and controls added.
    if (!wxDialog::Create(parent, id, title, pos, size, style, name))
    {
        return false;
    }

    auto* dlg_sizer = new wxBoxSizer(wxVERTICAL);

    m_splitter = new wxSplitterWindow(this, wxID_ANY, wxDefaultPosition, FromDIP(wxSize(400, -1)), wxSP_3D);
    m_splitter->SetSashGravity(0.0);
    m_splitter->SetMinimumPaneSize(600);
    dlg_sizer->Add(m_splitter, wxSizerFlags(1).Expand().Border(wxALL));

    m_markdown = new wxStyledTextCtrl(m_splitter);
    {
        m_markdown->SetLexer(wxSTC_LEX_MARKDOWN);
        m_markdown->SetEOLMode(wxSTC_EOL_LF);
        m_markdown->SetViewWhiteSpace(wxSTC_WS_VISIBLEAFTERINDENT);
        // Sets text margin scaled appropriately for the current DPI on Windows,
        // 5 on wxGTK or wxOSX

        m_markdown->SetMarginLeft(wxSizerFlags::GetDefaultBorder());
        m_markdown->SetMarginRight(wxSizerFlags::GetDefaultBorder());
        m_markdown->SetMarginWidth(1, 0);
        // Remove default margin

        m_markdown->SetTabWidth(4);
        m_markdown->SetBackSpaceUnIndents(true);
    }

    m_html = new wxStyledTextCtrl(m_splitter);
    {
        m_html->SetLexer(wxSTC_LEX_HTML);
        m_html->SetEOLMode(wxSTC_EOL_LF);
        m_html->SetViewWhiteSpace(wxSTC_WS_VISIBLEAFTERINDENT);
        // Sets text margin scaled appropriately for the current DPI on Windows,
        // 5 on wxGTK or wxOSX

        m_html->SetMarginLeft(wxSizerFlags::GetDefaultBorder());
        m_html->SetMarginRight(wxSizerFlags::GetDefaultBorder());
        m_html->SetMarginWidth(1, 0);
        // Remove default margin

        m_html->SetTabWidth(4);
        m_html->SetBackSpaceUnIndents(true);
    }
    m_splitter->SplitVertically(m_markdown, m_html);
    m_splitter->SetSashPosition(600);

    auto* stdBtn = CreateStdDialogButtonSizer(wxCLOSE|wxNO_DEFAULT);
    stdBtn->GetCancelButton()->SetDefault();
    dlg_sizer->Add(stdBtn, wxSizerFlags().Expand().Border(wxALL));

    SetMinSize(FromDIP(wxSize(1200, 1200)));
    if (pos != wxDefaultPosition)
    {
        // Now that the dialog is created, set the scaled position
        SetPosition(FromDIP(pos));
    }
    if (size == wxDefaultSize)
    {
        // If default size let the sizer set the dialog's size
        // so that it is large enough to fit it's child controls.
        SetSizerAndFit(dlg_sizer);
    }
    else
    {
        SetSizer(dlg_sizer);
        if (size.x == wxDefaultCoord || size.y == wxDefaultCoord)
        {
            // Use the sizer to calculate the missing dimension
            Fit();
        }
        SetSize(FromDIP(size));
        Layout();
    }
    Centre(wxBOTH);

    wxPersistentRegisterAndRestore(this, "ViewDetails_base");
    wxPersistentRegisterAndRestore(m_splitter, "info");

    // Event handlers
    Bind(wxEVT_INIT_DIALOG, &ViewDetails_base::OnInit, this);

    return true;
}

// ************* End of generated code ***********
// DO NOT EDIT THIS COMMENT BLOCK!
//
// Code below this comment block will be preserved
// if the code for this class is re-generated.
//
// NOLINTEND
// cppcheck-suppress-end *
// ***********************************************
// </auto-generated>
