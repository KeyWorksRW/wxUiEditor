# XML Structure and Schema

This document describes the XML structure and schema of `.wxui` files.

## Root Element

TODO: Document the root XML element structure, required attributes, and namespace declarations.

## Project Settings

TODO: Document project-level settings and metadata.

## Widget Hierarchy

TODO: Document how widgets are nested and organized in the XML structure.

## Property Elements

TODO: Document the format and naming conventions for property elements.

## Variable Naming

TODO: Document how the `variable` attribute maps to generated code member names.
