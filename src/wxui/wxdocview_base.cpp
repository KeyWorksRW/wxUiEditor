///////////////////////////////////////////////////////////////////////////////
// <auto-generated>
// Code generated by wxUiEditor - see https://github.com/KeyWorksRW/wxUiEditor/
//
// DO NOT EDIT ANYTHING IN THIS FILE UNTIL AFTER THE </auto-generated> MARKER
// All changes before that marker will be lost if the code is re-generated.
///////////////////////////////////////////////////////////////////////////////

// clang-format off
// NOLINTBEGIN
// cppcheck-suppress-begin *

#include <wx/artprov.h>
#include <wx/button.h>
#include <wx/persist/splitter.h>
#include <wx/sizer.h>

#include "wxdocview_base.h"

bool wxDocView_base::Create(wxWindow* parent, wxWindowID id, const wxString& title,
    const wxPoint& pos, const wxSize& size, long style, const wxString &name)
{
    // Scaling of pos and size are handled after the dialog
    // has been created and controls added.
    if (!wxDialog::Create(parent, id, title, pos, size, style, name))
    {
        return false;
    }

    auto* dlg_sizer = new wxBoxSizer(wxVERTICAL);
    dlg_sizer->SetMinSize(800, 1024);

    m_splitter2 = new wxSplitterWindow(this, wxID_ANY, wxDefaultPosition, wxDefaultSize, wxSP_3D);
    m_splitter2->SetSashGravity(0.0);
    m_splitter2->SetMinimumPaneSize(150);
    dlg_sizer->Add(m_splitter2, wxSizerFlags(1).Expand().Border(wxALL));

    auto* panel = new wxPanel(m_splitter2, wxID_ANY, wxDefaultPosition, wxDefaultSize, wxBORDER_THEME|wxTAB_TRAVERSAL);
    panel->SetMinSize(FromDIP(wxSize(175, -1)));

    auto* panel_sizer = new wxBoxSizer(wxVERTICAL);

    m_choicebook = new wxChoicebook(panel, wxID_ANY);
    panel_sizer->Add(m_choicebook, wxSizerFlags(1).Expand().Border(wxALL));

    m_search_page = new wxPanel(m_choicebook, wxID_ANY, wxDefaultPosition, wxDefaultSize, wxTAB_TRAVERSAL);
    m_choicebook->AddPage(m_search_page, "Search", true);
    m_search_page->SetBackgroundColour(wxSystemSettings::GetColour(wxSYS_COLOUR_BTNFACE));

    auto* box_sizer4 = new wxBoxSizer(wxVERTICAL);

    m_search_ctrl = new wxSearchCtrl(m_search_page, wxID_ANY, wxEmptyString, wxDefaultPosition, wxDefaultSize,
        wxTE_PROCESS_ENTER);
    m_search_ctrl->SetHint("Search documentation...");
    m_search_ctrl->ShowSearchButton(true);
    m_search_ctrl->ShowCancelButton(true);
    box_sizer4->Add(m_search_ctrl, wxSizerFlags().Expand().Border(wxALL));

    m_search_listbox = new wxListBox(m_search_page, wxID_ANY);
    box_sizer4->Add(m_search_listbox,
        wxSizerFlags(1).Expand().Border(wxLEFT|wxRIGHT|wxBOTTOM, wxSizerFlags::GetDefaultBorder()));
    m_search_page->SetSizerAndFit(box_sizer4);

    m_classes_page = new wxPanel(m_choicebook, wxID_ANY, wxDefaultPosition, wxDefaultSize, wxTAB_TRAVERSAL);
    m_choicebook->AddPage(m_classes_page, "Classes");
    m_classes_page->SetBackgroundColour(wxSystemSettings::GetColour(wxSYS_COLOUR_BTNFACE));

    auto* page_sizer = new wxBoxSizer(wxVERTICAL);

    m_classes_textctrl = new wxTextCtrl(m_classes_page, wxID_ANY, wxEmptyString, wxDefaultPosition, wxDefaultSize,
        wxTE_PROCESS_ENTER);
    page_sizer->Add(m_classes_textctrl, wxSizerFlags().Expand().Border(wxALL));

    m_classes_listbox = new wxListBox(m_classes_page, wxID_ANY);
    page_sizer->Add(m_classes_listbox, wxSizerFlags(1).Expand().Border(wxALL));
    m_classes_page->SetSizerAndFit(page_sizer);

    m_events_page = new wxPanel(m_choicebook, wxID_ANY, wxDefaultPosition, wxDefaultSize, wxTAB_TRAVERSAL);
    m_choicebook->AddPage(m_events_page, "Events");
    m_events_page->SetBackgroundColour(wxSystemSettings::GetColour(wxSYS_COLOUR_BTNFACE));

    auto* page_sizer2 = new wxBoxSizer(wxVERTICAL);

    m_events_textctrl = new wxTextCtrl(m_events_page, wxID_ANY, wxEmptyString, wxDefaultPosition, wxDefaultSize,
        wxTE_PROCESS_ENTER);
    page_sizer2->Add(m_events_textctrl, wxSizerFlags().Expand().Border(wxALL));

    m_events_listbox = new wxListBox(m_events_page, wxID_ANY);
    page_sizer2->Add(m_events_listbox, wxSizerFlags(1).Expand().Border(wxALL));
    m_events_page->SetSizerAndFit(page_sizer2);

    m_functions_page = new wxPanel(m_choicebook, wxID_ANY, wxDefaultPosition, wxDefaultSize, wxTAB_TRAVERSAL);
    m_choicebook->AddPage(m_functions_page, "Functions");
    m_functions_page->SetBackgroundColour(wxSystemSettings::GetColour(wxSYS_COLOUR_BTNFACE));

    auto* page_sizer4 = new wxBoxSizer(wxVERTICAL);

    m_functions_textctrl = new wxTextCtrl(m_functions_page, wxID_ANY, wxEmptyString, wxDefaultPosition, wxDefaultSize,
        wxTE_PROCESS_ENTER);
    page_sizer4->Add(m_functions_textctrl, wxSizerFlags().Expand().Border(wxALL));

    m_functions_listbox = new wxListBox(m_functions_page, wxID_ANY);
    page_sizer4->Add(m_functions_listbox, wxSizerFlags(1).Expand().Border(wxALL));
    m_functions_page->SetSizerAndFit(page_sizer4);

    m_overviews_page = new wxPanel(m_choicebook, wxID_ANY, wxDefaultPosition, wxDefaultSize, wxTAB_TRAVERSAL);
    m_choicebook->AddPage(m_overviews_page, "Overviews");
    m_overviews_page->SetBackgroundColour(wxSystemSettings::GetColour(wxSYS_COLOUR_BTNFACE));

    auto* page_sizer3 = new wxBoxSizer(wxVERTICAL);

    m_overviews_textctrl = new wxTextCtrl(m_overviews_page, wxID_ANY, wxEmptyString, wxDefaultPosition, wxDefaultSize,
        wxTE_PROCESS_ENTER);
    page_sizer3->Add(m_overviews_textctrl, wxSizerFlags().Expand().Border(wxALL));

    m_overviews_listbox = new wxListBox(m_overviews_page, wxID_ANY);
    page_sizer3->Add(m_overviews_listbox, wxSizerFlags(1).Expand().Border(wxALL));
    m_overviews_page->SetSizerAndFit(page_sizer3);
    panel->SetSizerAndFit(panel_sizer);

    auto* panel2 = new wxPanel(m_splitter2, wxID_ANY, wxDefaultPosition, wxDefaultSize, wxBORDER_THEME|wxTAB_TRAVERSAL);

    auto* box_sizer3 = new wxBoxSizer(wxVERTICAL);

    m_aui_tool_bar = new wxAuiToolBar(panel2, wxID_ANY, wxDefaultPosition, wxDefaultSize,
        wxAUI_TB_DEFAULT_STYLE|wxAUI_TB_PLAIN_BACKGROUND);
    m_home = m_aui_tool_bar->AddTool(wxID_ANY, wxEmptyString, wxArtProvider::GetBitmapBundle(wxART_GO_HOME, wxART_TOOLBAR,
        wxSize(24, 24)));
    m_back = m_aui_tool_bar->AddTool(wxID_ANY, wxEmptyString, wxArtProvider::GetBitmapBundle(wxART_GO_BACK, wxART_TOOLBAR,
        wxSize(24, 24)));
    m_forward = m_aui_tool_bar->AddTool(wxID_ANY, wxEmptyString, wxArtProvider::GetBitmapBundle(wxART_GO_FORWARD,
        wxART_TOOLBAR, wxSize(24, 24)));
    m_aui_tool_bar->AddTool(wxID_ANY, wxEmptyString, wxArtProvider::GetBitmapBundle(wxART_FIND, wxART_TOOLBAR,
        wxSize(30, 30)));
    m_aui_tool_bar->Realize();
    box_sizer3->Add(m_aui_tool_bar, wxSizerFlags().Expand().Border(wxALL));

    m_html_win = new wxHtmlWindow(panel2, wxID_ANY, wxDefaultPosition, wxDefaultSize, wxHW_SCROLLBAR_AUTO|wxBORDER_SUNKEN);
    m_html_win->SetMinSize(FromDIP(wxSize(300, 450)));
    box_sizer3->Add(m_html_win, wxSizerFlags(1).Expand().Border(wxALL));
    panel2->SetSizerAndFit(box_sizer3);
    m_splitter2->SplitVertically(panel, panel2);
    m_splitter2->SetSashPosition(75);

    auto* stdBtn = CreateStdDialogButtonSizer(wxCLOSE|wxNO_DEFAULT);
    stdBtn->GetCancelButton()->SetDefault();
    dlg_sizer->Add(stdBtn, wxSizerFlags().Expand().Border(wxALL));

    if (pos != wxDefaultPosition)
    {
        // Now that the dialog is created, set the scaled position
        SetPosition(FromDIP(pos));
    }
    if (size == wxDefaultSize)
    {
        // If default size let the sizer set the dialog's size
        // so that it is large enough to fit it's child controls.
        SetSizerAndFit(dlg_sizer);
    }
    else
    {
        SetSizer(dlg_sizer);
        if (size.x == wxDefaultCoord || size.y == wxDefaultCoord)
        {
            // Use the sizer to calculate the missing dimension
            Fit();
        }
        SetSize(FromDIP(size));
        Layout();
    }
    m_search_ctrl->SetFocus();

    Centre(wxBOTH);
    wxPersistentRegisterAndRestore(m_splitter2, "rtf_html_sash");

    // Event handlers
    m_choicebook->Bind(wxEVT_CHOICEBOOK_PAGE_CHANGED, &wxDocView_base::OnPageChanged, this);
    m_html_win->Bind(wxEVT_HTML_LINK_CLICKED, &wxDocView_base::OnHtmlLink, this);
    Bind(wxEVT_INIT_DIALOG, &wxDocView_base::OnInit, this);
    m_classes_textctrl->Bind(wxEVT_KEY_DOWN, &wxDocView_base::OnTextKeyDown, this);
    m_events_textctrl->Bind(wxEVT_KEY_DOWN, &wxDocView_base::OnTextKeyDown, this);
    m_functions_textctrl->Bind(wxEVT_KEY_DOWN, &wxDocView_base::OnTextKeyDown, this);
    m_overviews_textctrl->Bind(wxEVT_KEY_DOWN, &wxDocView_base::OnTextKeyDown, this);
    m_classes_listbox->Bind(wxEVT_LISTBOX_DCLICK, &wxDocView_base::OnDblClickListBox, this);
    m_events_listbox->Bind(wxEVT_LISTBOX_DCLICK, &wxDocView_base::OnDblClickListBox, this);
    m_functions_listbox->Bind(wxEVT_LISTBOX_DCLICK, &wxDocView_base::OnDblClickListBox, this);
    m_overviews_listbox->Bind(wxEVT_LISTBOX_DCLICK, &wxDocView_base::OnDblClickListBox, this);
    m_search_listbox->Bind(wxEVT_LISTBOX_DCLICK, &wxDocView_base::OnDisplaySearchListItem, this);
    m_search_ctrl->Bind(wxEVT_SEARCHCTRL_CANCEL_BTN, &wxDocView_base::OnSearchCancel, this);
    m_classes_textctrl->Bind(wxEVT_TEXT, &wxDocView_base::OnIndexTextChange, this);
    m_events_textctrl->Bind(wxEVT_TEXT, &wxDocView_base::OnIndexTextChange, this);
    m_functions_textctrl->Bind(wxEVT_TEXT, &wxDocView_base::OnIndexTextChange, this);
    m_overviews_textctrl->Bind(wxEVT_TEXT, &wxDocView_base::OnIndexTextChange, this);
    m_search_ctrl->Bind(wxEVT_TEXT, &wxDocView_base::OnSearchTextChanged, this);
    m_classes_textctrl->Bind(wxEVT_TEXT_ENTER, &wxDocView_base::OnIndexTextEnter, this);
    m_events_textctrl->Bind(wxEVT_TEXT_ENTER, &wxDocView_base::OnIndexTextEnter, this);
    m_functions_textctrl->Bind(wxEVT_TEXT_ENTER, &wxDocView_base::OnIndexTextEnter, this);
    m_overviews_textctrl->Bind(wxEVT_TEXT_ENTER, &wxDocView_base::OnIndexTextEnter, this);
    Bind(wxEVT_TOOL, &wxDocView_base::OnHome, this, m_home->GetId());

    return true;
}

// ************* End of generated code ***********
// DO NOT EDIT THIS COMMENT BLOCK!
//
// Code below this comment block will be preserved
// if the code for this class is re-generated.
//
// NOLINTEND
// cppcheck-suppress-end *
// ***********************************************
// </auto-generated>
